docker run -d -v /home/docker/test:/root -p 3000:3000 --name Abathur -w /root python:2-slim  /bin/sh -c 'pip install Flask && echo "from flask import Flask\napp = Flask(__name__)\n\n@app.route(\"/\")\ndef hello_world():\n\treturn \"<h1>Hello, World</h1>\"\n" > hello.py &&  export FLASK_APP=hello.py && flask run --host=0.0.0.0 --port=3000'
